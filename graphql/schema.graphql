type Query {
  owner: Owner!
  friends: [Friend]!
  friend(id: Int!): Friend!
}

type Owner {
  age: Int!
  email: String!
  university: String!
}

type Friend {
  id: Int!
  name: String!
  age: Int!
  email: String!
  company: String!
}
